# =============================================================================
# 환경 변수 예시 - .env로 복사 후 값 채우기
# =============================================================================

# -----------------------------------------------------------------------------
# 데이터베이스 (Vercel 배포 시 필수 - PostgreSQL)
# -----------------------------------------------------------------------------
# 로컬 개발: Neon, Supabase, Vercel Postgres 등 PostgreSQL 연결 문자열 사용
# 예: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# -----------------------------------------------------------------------------
# Vercel Blob (선택 - Vercel 배포 시 파일 업로드용)
# -----------------------------------------------------------------------------
# Vercel 대시보드 > Storage > Blob 생성 후 자동 주입되거나,
# 로컬에서는 vercel env pull 로 가져오기
# BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# -----------------------------------------------------------------------------
# Gemini API (선택 - Explore 피드용)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_GEMINI_API_KEY="..."
# 또는 NEXT_PUBLIC_API_KEY="..."
