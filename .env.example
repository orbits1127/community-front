# Environment variables for Prisma
# Copy this file to .env and update with your actual values

# PostgreSQL connection string format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA

DATABASE_URL="postgresql://postgres:password@localhost:5432/instagram_db?schema=public"

# For development with SQLite (simpler setup, no PostgreSQL required):
# Uncomment the line below and comment out the PostgreSQL URL above
# Also change provider in schema.prisma from "postgresql" to "sqlite"
# DATABASE_URL="file:./dev.db"
